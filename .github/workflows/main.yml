name: Gerar APK

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar codigo do repositorio
        uses: actions/checkout@v4

      - name: Instalar Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Configurar Python Estavel (3.11)
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar ferramentas de compilacao do sistema
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential ccache cmake git libffi-dev libssl-dev zip unzip lld libncurses-dev autoconf automake libtool libltdl-dev pkg-config zlib1g-dev gawk patch libsqlite3-dev libbz2-dev gettext

      - name: Instalar Buildozer Estável
        run: |
          pip install --upgrade pip setuptools wheel virtualenv "cython<=3.0.12"
          pip install buildozer

      # 1. Clona o python-for-android explicitamente
      - name: Clonar python-for-android
        run: |
          git clone https://github.com/kivy/python-for-android.git python-for-android

      # 2. Injeta o servico no template clonado
      - name: Injetar servico no template do p4a
        run: |
          python3 -c '
          file_path = "python-for-android/pythonforandroid/bootstraps/sdl2/build/templates/AndroidManifest.tmpl.xml"
          service_xml = """
                  <service android:name="org.exemplo.gastosteste.NotificationReceiver"
                           android:label="Meu App de Gastos"
                           android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
                           android:exported="true">
                      <intent-filter>
                          <action android:name="android.service.notification.NotificationListenerService" />
                      </intent-filter>
                  </service>
          """
          with open(file_path, "r") as f:
              content = f.read()

          if "</application>" in content:
              new_content = content.replace("</application>", f"{service_xml}\n    </application>")
              with open(file_path, "w") as f:
                  f.write(new_content)
              print("✅ SERVIÇO INJETADO COM SUCESSO NO TEMPLATE CLONADO!")
          else:
              print("❌ ERRO: Tag </application> nao encontrada no template!")
          '

      # 3. Compila o APK usando o p4a.source_dir configurado no spec
      - name: Compilar APK com Buildozer
        env:
          CFLAGS: "-Wno-error=incompatible-pointer-types"
        run: |
          yes | buildozer -v android debug

      # 4. Inspeciona o manifesto final
      - name: Inspecionar AndroidManifest.xml FINAL do App
        run: |
          find .buildozer -path "*/dists/*" -name "AndroidManifest.xml" -exec cat {} +

      - name: Disponibilizar APK para Download
        uses: actions/upload-artifact@v4
        with:
          name: meu-aplicativo-debug
          path: bin/*.apk
          
